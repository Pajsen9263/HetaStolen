# HetaStolen

Caiban here are some instructions for getting setup:

1. Have bun installed.
2. Run `bun install`
3. Copy the .env.example to a .env and input your own database file name
4. Run `bun run drizzle:push`
5. Then you can run the dev server forever `bun run dev`
